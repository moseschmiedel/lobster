services:
  lobster-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:3000'
    volumes:
      - /root/songs:/songs
    environment:
      - PUBLIC_SONGS_PATH=/songs
