services:
  lobster-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - /root/songs:/songs
    environment:
      - PUBLIC_SONGS_PATH=/songs
